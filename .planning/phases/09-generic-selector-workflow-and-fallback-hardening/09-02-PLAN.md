---
phase: 09-generic-selector-workflow-and-fallback-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/scraper/services/countries/index.js
  - src/scraper/services/leagues/index.js
  - src/scraper/services/seasons/index.js
  - src/selector-health/contracts/index.js
autonomous: true
requirements:
  - SCRP-01
  - RELY-01
  - RELY-02
user_setup: []
must_haves:
  truths:
    - "Country discovery returns complete generic country options from soccer routes instead of only visible menu subsets"
    - "League and season extraction remain generic and recover when primary selectors yield empty entity lists"
    - "Shared selector contracts prefer league/season-specific anchors while preserving fallback resilience"
  artifacts:
    - "src/scraper/services/countries/index.js merges primary selector results with broad soccer-link fallback filtering"
    - "src/scraper/services/leagues/index.js keeps generic parsing and adds fallback selector passes for empty primary results"
    - "src/scraper/services/seasons/index.js collects season links via primary and fallback selectors while preserving league filtering"
    - "src/selector-health/contracts/index.js narrows league selector contract to league-specific anchor patterns"
  key_links:
    - "countries service output -> league target discovery -> generic scope probing coverage"
    - "league service output -> season archive discovery -> downstream match list/detail scope integrity"
    - "selector contract specificity + service fallbacks -> stable discovery even when menu layout visibility changes"
---

<objective>
Harden generic country/league/season discovery and shared selector contract fallbacks.

Purpose: prevent false selector-health confidence caused by partial menu visibility and keep extraction resilient when UI containers change.
Output: robust generic discovery services plus refined contract selectors that continue to work across countries/leagues/seasons.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/scraper/services/countries/index.js
@src/scraper/services/leagues/index.js
@src/scraper/services/seasons/index.js
@src/selector-health/contracts/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand country discovery to combine generic selector and broad soccer-link fallback sources</name>
  <files>src/scraper/services/countries/index.js</files>
  <action>Update country discovery evaluation so it collects `/soccer/{country}/` anchors from both primary contract selector results and broad soccer-link fallback queries, then deduplicates by slug. Preserve strict filtering so only country-level paths are accepted and keep selector diagnostics attached.</action>
  <verify><automated>node --check src/scraper/services/countries/index.js</automated></verify>
  <done>Country discovery returns complete generic country coverage without depending on a partially visible left-menu subset.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen league and season discovery fallback collection while keeping generic filters</name>
  <files>src/scraper/services/leagues/index.js, src/scraper/services/seasons/index.js</files>
  <action>Ensure league and season services attempt primary selector extraction first, then execute fallback selector passes when primary results are empty. Keep path-segment filtering generic (sport/country/league constraints) so discovery remains reusable across all entities. Preserve stable return contracts and diagnostics attachment.</action>
  <verify><automated>node --check src/scraper/services/leagues/index.js src/scraper/services/seasons/index.js</automated></verify>
  <done>League/season discovery yields valid entity lists for representative countries and remains resilient to layout/container drift.</done>
</task>

<task type="auto">
  <name>Task 3: Refine shared league selector contract and run representative extraction sanity checks</name>
  <files>src/selector-health/contracts/index.js, src/scraper/services/countries/index.js, src/scraper/services/leagues/index.js, src/scraper/services/seasons/index.js</files>
  <action>Adjust league selector contract candidates toward league-specific anchors and validate extraction chain sanity with lightweight runtime checks (countries -> argentina leagues -> representative season count). Confirm behavior aligns with generic selector philosophy: fix once at contract/service layer, apply everywhere.</action>
  <verify><automated>npm run health:selectors -- --scope leagues --sample 2 --quiet</automated></verify>
  <done>Shared selector contracts and discovery services produce stable generic results, and representative scope checks pass after contract tightening.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --check src/scraper/services/countries/index.js src/scraper/services/leagues/index.js src/scraper/services/seasons/index.js src/selector-health/contracts/index.js
- [ ] npm run health:selectors -- --scope leagues --sample 2 --quiet
- [ ] node --input-type=module -e "import { chromium } from 'playwright'; import { getListOfCountries } from './src/scraper/services/countries/index.js'; import { getListOfLeagues } from './src/scraper/services/leagues/index.js'; import { getListOfSeasons } from './src/scraper/services/seasons/index.js'; const browser = await chromium.launch({ headless: true }); const context = await browser.newContext(); try { const countries = await getListOfCountries(context); const leagues = await getListOfLeagues(context, 'argentina'); const league = leagues.find((entry) => entry.url.includes('/liga-profesional/')) || leagues[0]; const seasons = league ? await getListOfSeasons(context, league.url) : []; if (!Array.isArray(countries) || countries.length < 100) process.exit(1); if (!Array.isArray(leagues) || leagues.length < 5) process.exit(1); if (!Array.isArray(seasons) || seasons.length < 1) process.exit(1); } finally { await context?.close(); await browser?.close(); }"
</verification>

<success_criteria>

- All plan tasks completed
- Generic country discovery returns broad country coverage and no longer depends on truncated menu visibility
- Generic league/season extraction remains stable with fallback collection under selector/container drift
- Representative runtime chain (country -> league -> season) confirms selector/fallback hardening works end-to-end

</success_criteria>

<output>
After completion, create .planning/phases/09-generic-selector-workflow-and-fallback-hardening/09-02-SUMMARY.md
</output>
