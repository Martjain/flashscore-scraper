---
phase: 09-generic-selector-workflow-and-fallback-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/arguments/index.js
  - scripts/health-selectors.mjs
  - src/selector-health/health-check/runSelectorHealthCheck.js
  - src/selector-health/health-check/reporting.js
autonomous: true
requirements:
  - SCRP-01
  - RELY-01
  - RELY-02
user_setup: []
must_haves:
  truths:
    - "Operators can run a generic selector workflow that validates representative country/league/season paths using shared selectors"
    - "Selector health CLI clearly distinguishes generic mode from sample/all modes and prevents ambiguous flag combinations"
    - "Generic mode still preserves deterministic fallback discovery so selectors can be repaired at scope level, not entity by entity"
  artifacts:
    - "src/cli/arguments/index.js parses `--generic` / `--pick-any` and enforces compatible argument combinations"
    - "scripts/health-selectors.mjs documents generic mode and reports target mode metadata in failure payloads"
    - "src/selector-health/health-check/runSelectorHealthCheck.js resolves representative targets per scope and propagates target mode"
    - "src/selector-health/health-check/reporting.js renders generic-mode summary output for operators"
  key_links:
    - "CLI flags -> parseSelectorHealthArguments -> runSelectorHealthCheck option contract"
    - "target mode and representative selection -> scope probe target builder -> selector probe execution"
    - "run metadata (`targetMode`, seed/sample) -> reporting summary -> operator troubleshooting loop"
---

<objective>
Implement generic selector health execution mode for scope-level selector validation.

Purpose: align selector diagnostics with shared generic selectors so one fix applies to every country/league/season that uses the same contract.
Output: health CLI + probe runner updates that support representative-path selection with explicit operator-facing mode reporting.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/arguments/index.js
@scripts/health-selectors.mjs
@src/selector-health/health-check/runSelectorHealthCheck.js
@src/selector-health/health-check/reporting.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit generic selector flags and guardrails in selector-health CLI arguments</name>
  <files>src/cli/arguments/index.js, scripts/health-selectors.mjs</files>
  <action>Add argument support for `--generic` (alias `--pick-any`) in selector health parsing, reject invalid mixes with `--sample`, and preserve strict unknown-option handling. Update help text so operators understand when to use generic mode vs sample/all. Ensure failure payload metadata captures target mode consistently so alert/diagnostic paths stay coherent.</action>
  <verify><automated>node --check src/cli/arguments/index.js scripts/health-selectors.mjs</automated></verify>
  <done>Selector health CLI accepts generic mode intentionally, rejects ambiguous inputs, and exposes clear mode semantics to both terminal output and artifact payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Implement representative target resolution for generic selector workflow</name>
  <files>src/selector-health/health-check/runSelectorHealthCheck.js</files>
  <action>Extend scope target-building logic so `pickAny/generic` mode chooses one representative discovered entity per scope chain (country -> league -> season -> match) while retaining shared selector contracts. Keep discovery deterministic per run seed and route through existing fallback/static target behavior when discovery is sparse. Propagate `targetMode` and selection metadata in run results.</action>
  <verify><automated>node --check src/selector-health/health-check/runSelectorHealthCheck.js</automated></verify>
  <done>Generic mode probes a representative discovered path per scope and still returns structured diagnostics when upstream discovery is partial or unavailable.</done>
</task>

<task type="auto">
  <name>Task 3: Surface generic mode in health summaries and verify end-to-end generic probes</name>
  <files>src/selector-health/health-check/reporting.js, scripts/health-selectors.mjs</files>
  <action>Update summary rendering so operators can see when a run used generic mode rather than sample/all. Validate end-to-end behavior with dry-run and live scope runs for countries/leagues/seasons under generic mode. Keep compatibility with existing quiet/default output paths.</action>
  <verify><automated>npm run health:selectors -- --dry-run --scope countries --scope leagues --scope seasons --generic</automated></verify>
  <done>Operator output explicitly shows generic-mode behavior, and generic probes execute successfully for all discovery scopes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --check src/cli/arguments/index.js scripts/health-selectors.mjs src/selector-health/health-check/runSelectorHealthCheck.js src/selector-health/health-check/reporting.js
- [ ] npm run health:selectors -- --dry-run --scope countries --scope leagues --scope seasons --generic
- [ ] npm run health:selectors -- --scope countries --generic --quiet
- [ ] npm run health:selectors -- --scope leagues --generic --quiet
- [ ] npm run health:selectors -- --scope seasons --generic --quiet
</verification>

<success_criteria>

- All plan tasks completed
- Generic mode (`--generic`/`--pick-any`) is available and validated for countries, leagues, and seasons scopes
- Selector-health artifacts and terminal summaries expose target mode clearly for operator debugging
- Generic workflow keeps selector validation focused on shared contracts and fallback behavior

</success_criteria>

<output>
After completion, create .planning/phases/09-generic-selector-workflow-and-fallback-hardening/09-01-SUMMARY.md
</output>
